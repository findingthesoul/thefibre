/**
 * The single place the displayed version lives.
 *
 * Shown in the Fibre sidebar footer and on Settings → How The Fibre works.
 * Bump it in the same commit as the CHANGELOG entry and the package.json
 * files — see CLAUDE.md, "Version bumps".
 */
export const VERSION = '0.48.0';
