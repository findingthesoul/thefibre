import { EmptyState } from '@/components/ui/page';

export default function ChangeTab() {
  return (
    <EmptyState>
      Change context tab — coming soon. (Role in change, blockers, motivators, facilitator notes.)
    </EmptyState>
  );
}
