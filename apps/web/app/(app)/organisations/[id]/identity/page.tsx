import { EmptyState } from '@/components/ui/page';

export default function IdentityTab() {
  return (
    <EmptyState>
      Identity tab — coming soon. (Mission, vision, values, governance, decision-making style.)
    </EmptyState>
  );
}
