import { EmptyState } from '@/components/ui/page';

export default function OrgRelationshipTab() {
  return (
    <EmptyState>
      Relationship tab — coming soon. (Stage, health, engagement type, programmes completed, next opportunity.)
    </EmptyState>
  );
}
