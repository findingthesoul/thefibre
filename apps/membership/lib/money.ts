// Membership's binding of the shared money helpers (@thefibre/shared/money,
// one copy since 2026-09-14). The shared defaults ARE Membership's behaviour:
// whole amounts drop the decimals, a €19,50 tier keeps them; nl-NL default.
export { money, formatPeriod } from '@thefibre/shared/money';
