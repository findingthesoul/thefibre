// Re-export of the shared component — the implementation lives in
// packages/shared/src/ui/date-field.tsx (single source of truth; the
// per-app copies drifted, see v0.13.104).
export { DateField, DateTimeField } from '@thefibre/shared/ui/date-field';
