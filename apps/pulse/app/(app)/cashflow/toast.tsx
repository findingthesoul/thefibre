'use client';

// Shim: the toast store + stack live in @thefibre/shared/ui/toast
// (extraction phase 2 — this was the original copy).

export { toastError, ToastStack } from '@thefibre/shared/ui/toast';
