# Handover — 2026-09-05 (end of the Membership marathon sessions)

_For the next Claude session. Read CLAUDE.md first (it has the binding
rules), then this for where things stand RIGHT NOW. The auto-memory and
docs/build-plan.md are current; this file is the short bridge._

## State: everything is shipped and green

- **Version**: v0.39.1 (root) · Membership 0.6.0 · Thread 3.33.0 ·
  Meet 2.4.3. Committed through `c680d86`, pushed to `main` AND
  `main:staging`, both Fly APIs deployed, all migrations applied to BOTH
  databases, both smoke suites all green. Nothing is uncommitted, no
  background agents are running.
- **The Membership app is complete for v1**: tiers, products (which CARRY
  their access grants — Circle spaces, Threads, Fibre seats with
  approval policy + billed-seat consent), pricing LOGIC BUILDER
  (Settings → Pricing rules: when-country-is-one-of → pct rows,
  self-declared country on the join page, server-authoritative,
  reprice-from-next-renewal on country change, card-mismatch admin
  email), member self-serve portal (/my), invoices, embeds, canonical
  settings hub, drag-and-drop ordering everywhere.
- **Platform gains this week**: shared components (invoices area,
  settings hub, currency editor, search-select, date-field usage,
  extraction phase 1 = button/dialog/switch/list/fields/theme-script/
  app-switcher — ~1,640 lines removed), workspace currency SPoT (+ECB
  rates endpoint), per-app access roles on the Members page (list +
  dialogs), one-profile convergence (Fibre edits identity; Meet/Thread
  show read-only echoes; booking pages read platform profile first),
  per-event images in threads, env-aware app links (appUrl, NEVER
  APPS[slug].url or catalogue base_url — staging leaked users to prod).
- **Circle SSO spike**: /api/v1/oauth {authorize, continue, token, me}
  built + docs/spike-circle-sso.md. DELIBERATELY UNPLUGGED — no
  oauth_client rows exist. Do NOT touch Circle's SSO screen until a
  staging test with Sjoerd (misconfig locks his community's door).

## THE NEXT ACTION (decided, ready, not started)

**i18n P1 + P2** — D1–D5 decided 2026-09-05 (docs/i18n-proposal.md §5):
- Shared typed-catalog module at `@thefibre/shared` `./i18n`; LOCALES =
  en/nl/es/pt/de/**fr** (D4 overridden: FRENCH NOW).
- Add FR to every existing catalog (thread lib/i18n.ts ~73 keys, the
  email tables in apps/api/src/lib/email/thread-templates.ts) —
  machine-draft, mark `// MT` for native review.
- **Split thread.language's two jobs** (D1 sharpening): facilitation
  language (informational, organiser's) vs page/system language
  (chrome). Two fields; fresh migration to widen the language CHECK for
  'fr' (NEVER edit an applied migration).
- P1: Membership public surfaces (join page, embeds, /my) + membership
  + certificate emails ×6. P2: auth/platform emails + user-level UI
  language (user_profile.locale + savePref cookie, one setting
  app-wide).
- Estimated 3–5 sessions; parallelize with strict file lanes (shared
  module first, then surfaces).

## Also queued (build-plan.md is the SPoT, groomed)

- À-la-carte product buying (products have prices, no standalone
  checkout — reuse join-flow machinery with mode:'payment').
- Extraction phases 2–4 (docs/component-inventory.md — run in order).
- Workspace threads: docs/brief-workspace-threads.md — **D1–D3 STILL
  AWAIT SJOERD** (he decided i18n's five only; don't confuse the two
  D-lists).
- SearchSelect adoption sweep (timezone pickers, the 3 comboboxes).
- Org memberships with seats (§3.5, columns prepped).

## Waiting on Sjoerd (not code)

1. **Stripe Connect webhook** — THE blocker for real membership revenue:
   delete the wrong account-level endpoint, recreate choosing "listen on
   Connected accounts" AT CREATION (can't be flipped), five events, then
   `fly secrets set -a thefibre-api STRIPE_MEMBERSHIP_WEBHOOK_SECRET=…`
   (+ staging twin for the test-card rehearsal).
2. Workspace-threads D1–D3.
3. soul.com workspace: create, activate Membership, connect its Stripe,
   Circle token, enter the pricing rows (ZA 75 / AO 50 / default 100).
4. Circle SSO staging test, together.

## Working agreements learned this week (beyond CLAUDE.md)

- Sjoerd live-tests on staging and fires screenshots; fix + ship both
  branches within minutes. Every ship: nine package.json bumps + web
  version.ts + per-app VERSION where relevant + CHANGELOG, push main AND
  main:staging, fly deploy both when API changed.
- When "X doesn't work": check RLS WRITE paths first (three latent bugs
  this week were exactly that: app_membership grant, meet_root_slug
  triggers, plus env paste-errors found by scripts/verify-vercel-env.mjs).
- "Done" means VISIBLE, not wired — if the nav/labels still show the old
  model, he'll rightly call it not done.
- He upgrades designs mid-build (logic builder, products-carry-access);
  fold the upgrade in rather than shipping the narrower version.
- Parallel agents: strict disjoint file lanes, pre-wire shared
  touchpoints (server.ts stubs) yourself, assign migration timestamps
  explicitly, one combined typecheck + ONE release at the end.
- macOS has no `timeout`; `fly logs --no-tail` works. Fly deploy output
  often swallowed by tail — verify with `fly releases`.
