# Handover — 2026-09-07 (pause after the i18n/mobile/domains marathon)

_For the next session. Read CLAUDE.md first, then this. build-plan.md and
the auto-memory are current; this is the short bridge._

## State: everything is shipped, tested and green

- **Version v0.54.0**, main == staging, tree clean, no agents running,
  both Fly APIs deployed, both smoke suites green, `pnpm verify` (the new
  standing gate — run it before EVERY release) passes locally.
- **Three parallel sessions built this in ~36h**, coordinated via a
  serialization protocol (see memory: feedback-parallel-sessions —
  announce RELEASING NOW / released <sha>, pull before bumping, stage
  only your own paths, verify imports resolve in-commit, pnpm verify):
  - i18n **P1+P2+P3 complete**: six locales (en/nl/es/pt/de/fr) across
    every public surface, every email, and every signed-in screen of all
    six apps (~3,000 catalog keys). ONE user setting (Settings → Profile),
    cookie + identity_profile.locale in lockstep (atomic save + self-heal
    on the profile page, v0.51.1).
  - **One shared embed integration** (@thefibre/shared/embed-loader; both
    apps serve /embed.js from it) and **one shared shell** with the
    **mobile bottom menu** in all six apps; dialogs are sheets on phones.
  - **Domain flip done** (v0.52.0): apps on thethread.app subdomains,
    fibre stays thefibre.app, cross-apex SSO hop (crossAppHref — use it
    for cross-app links, never raw appUrl concatenation across apexes).
  - Org seats, P4 metering + archive gate, invoiced manual adds
    (Membership + Thread), à-la-carte + optional add-on products, Google
    Workspace grants, CORS-from-registry, Vercel skip-unchanged-builds
    (correct for multi-commit pushes), testing infra (48 tests).

## Standing rules added this weekend (beyond CLAUDE.md)

- **Every NEW user-facing string goes through a catalog ×6** — app chrome
  in lib/i18n-ui.ts, public surfaces in lib/i18n.ts, shared components
  via chromeT (client) / chrome-server-i18n (server). Never hardcode EN.
- API errors stay English (P4 never); /admin, the about explainer, and
  invoice legal documents are deliberately English.
- `pnpm verify` before releases; the parallel-session protocol above.

## Open queue (build-plan.md is the SPoT, groomed)

- **MT burn-down**: `grep -rn '// MT'` = the native-review list (~2,400+
  lines es/pt/de/fr). NL is native-quality but unreviewed by Sjoerd.
  Known calls for the review: ES "Settings" (Ajustes vs Configuración),
  PT dialect (everything is pt-BR incl. INTL_LOCALES.pt — flip both
  together if moving to pt-PT).
- P3 leftovers by design: sign-in/no-access/invite pages, Meet's public
  booking pages, static metadata titles, the about explainer.
- Mobile per-screen polish as phone testing surfaces it (build-plan 3c);
  builders stay desktop-first.
- Testing roadmap phase 3+ (thefibre-df's lane; CI token is the blocker).
- Unify per-app money() locale behavior (pulse kept nl-NL currency
  display; membership went locale-aware) — small consistency pass.

## Waiting on Sjoerd (not code)

1. **Test the six-language platform** — pick Nederlands in Settings →
   Profile on thefibre.app (or .tech), walk every app, desktop AND phone
   (bottom menu). Opening Settings → Profile auto-heals the one known
   stale-language state.
2. NL translation read + the two review calls above.
3. Production Stripe Connect webhook + soul.com workspace setup + Circle
   SSO staging test (unchanged, pre-marathon items).
